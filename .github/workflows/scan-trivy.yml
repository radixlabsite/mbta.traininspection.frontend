name: ⚙️ Scan Code with Trivy

on:
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  security-scan:
    name: 🛡️ Scan Code with Trivy
    uses: ./.github/workflows/reusable-trivy-scan.yml
    with:
      severity: 'HIGH,CRITICAL'
