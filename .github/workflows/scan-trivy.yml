name: ⚙️ CI/CD - Frontend Applications Development

on:
  workflow_dispatch:       

permissions:
  contents: read
  security-events: write   # Necessário para fazer o upload do SARIF
  actions: read            # Necessário para o upload do SARIF ler dados do run


jobs:

  security-scan:
    name: 🛡️ Scan Code with Trivy
    uses: ./.github/workflows/reusable-trivy-scan.yml
    with:
      severity: 'HIGH,CRITICAL'


