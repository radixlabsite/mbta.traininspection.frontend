import styled from 'styled-components';
import { metrics } from '@repo/themes';

export const Container = styled.div`
  padding-bottom: ${metrics.smallPadding};
  margin-bottom: ${metrics.xxLargePadding};
`